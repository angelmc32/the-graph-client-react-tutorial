query GetManySwaps {
  swaps(orderBy: timestamp, first: 1000, orderDirection: desc) {
    id
    amountUSD
    pair {
      token0 {
        name
        symbol
      }
      token0Price
      token1 {
        name
        symbol
      }
      token1Price
    }
    timestamp
  }
}
